node_modules
.DS_Store
*.local

# 빌드 산출물
dist
.astro
.wrangler
.vercel

# 생성물. `bun install` 의 postinstall 과 `bun run build` 가 `bun run gen` 으로 다시 만든다.
# `src/locales` 는 여기 없다 - 키는 추출기가 만들지만 값(번역문)은 사람이 채운다.
src/@types
src/common/lib/favicon-hosts.gen.ts
public/favicons
public/resume-*.pdf

# env
.env
.env.*
!.env.example

# 테스트 / storybook
storybook-static
*storybook.log
playwright-report
test-results
blob-report
.playwright-mcp
coverage
