# Mock Service Worker. 목킹할 게 생기면 true 로. 켜두면 서비스워커가 모든 요청을 경유시킨다.
PUBLIC_ENABLE_MSW=false

# 1로 두면 dev에서 React Query devtools 오버레이가 뜬다.
PUBLIC_DEVTOOLS=

# MonoLisa 를 받아올 fine-grained PAT (rhseung-assets 에 Contents: Read-only).
# 비우면 폰트를 안 받고 코드 서체가 Monaspace 로 폴백한다.
FONTS_TOKEN=
