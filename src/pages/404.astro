---
import { NotFoundIsland } from '@/pages/_islands/not-found-island';
import { DEFAULT_LANGUAGE } from '@/common/lib';
import Layout from '@/layouts/layout.astro';
---

<Layout lang={DEFAULT_LANGUAGE} title="404">
  <NotFoundIsland lang={DEFAULT_LANGUAGE} client:load />
</Layout>
